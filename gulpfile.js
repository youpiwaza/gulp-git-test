// // Resources
// Plugins
const {
  parallel,
  task,
} = require('gulp');

const git = require('gulp-git');

// // Tasks

// Default tesk, executed when using 'gulp'
// task('default', parallel('copy-assets'));
